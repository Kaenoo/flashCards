{
  "name": "vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && git add -f dist && git commit -m \"mise à jour dist\" && git push origin --delete gh-pages && git subtree push --prefix dist origin gh-pages"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.17",
    "@vitejs/plugin-vue": "^5.2.1",
    "gh-pages": "^6.3.0",
    "tailwindcss": "^4.0.17",
    "vite": "^6.2.0"
  }
}